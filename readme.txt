% Codes for Parallel Computing using MPI 

% Series code : series.c
% Parallel code (Basic) : parallel_new.c
% Parallel code (Using MPi_Reduce) : reduce_new.c

To run mpi code
Start with compiling the code : 
" mpicc <program_name>"
Then execute the code :
" mpirun -np 3 a.out"

To run C code :
"gcc <program_name>"
"./a.out"
